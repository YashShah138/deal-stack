---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.mjs
  - tailwind.config.ts
  - postcss.config.mjs
  - vitest.config.ts
  - .env.local.example
  - .gitignore
  - src/app/layout.tsx
  - src/app/page.tsx
  - supabase/migrations/00001_initial_schema.sql
  - supabase/config.toml
autonomous: true
requirements:
  - DB-01
  - DB-02
  - DB-03
  - DB-04
  - DB-05
  - DB-06
  - DB-07
  - DB-08
  - AUTH-04
  - AUTH-06

must_haves:
  truths:
    - "Next.js 14 app boots at localhost:3000 with TypeScript and Tailwind"
    - "All 11 database tables exist in the public schema with correct columns"
    - "RLS is enabled on every table in the public schema"
    - "Every user-scoped table has SELECT/INSERT/UPDATE/DELETE policies referencing auth.uid()"
    - "api_cache and api_usage_log have global authenticated-read policies with no user_id FK"
    - "Vitest is configured and can run (even with zero tests)"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with all Phase 1 core dependencies"
      contains: "next"
    - path: "supabase/migrations/00001_initial_schema.sql"
      provides: "Complete database schema with all 11 tables, RLS, policies, triggers"
      min_lines: 200
    - path: "vitest.config.ts"
      provides: "Vitest config targeting src/lib/engine with 100% coverage thresholds"
      contains: "defineConfig"
  key_links:
    - from: "supabase/migrations/00001_initial_schema.sql"
      to: "auth.users"
      via: "REFERENCES auth.users(id) ON DELETE CASCADE"
      pattern: "REFERENCES auth.users\\(id\\)"
---

<objective>
Scaffold the Next.js 14 project from scratch and create the complete Supabase database schema with RLS policies on every table.

Purpose: Every subsequent plan depends on the project existing and the database schema being in place. This is the absolute foundation.
Output: A bootable Next.js 14 app with Supabase schema migration ready to apply, Vitest configured for TDD in Plan 01-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 14 project with all core dependencies</name>
  <files>
    package.json, tsconfig.json, next.config.mjs, tailwind.config.ts, postcss.config.mjs,
    vitest.config.ts, .env.local.example, .gitignore, src/app/layout.tsx, src/app/page.tsx
  </files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md (lines 86-99 for install commands, lines 356-382 for vitest config, lines 994-1005 for next.config.mjs)
  </read_first>
  <action>
    1. Run `npx create-next-app@14.2.35 . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"` in the project root (which is currently empty except .planning and .git).
       - If create-next-app refuses to run in a non-empty dir, use a temp dir and move files back.

    2. Install core dependencies:
       ```
       npm install @supabase/supabase-js@2 @supabase/ssr@0 decimal.js@10 zod@3
       ```

    3. Install dev dependencies:
       ```
       npm install -D vitest @vitest/coverage-v8 @vitejs/plugin-react vite-tsconfig-paths
       ```

    4. Add test scripts to package.json:
       ```json
       "test": "vitest run",
       "test:watch": "vitest",
       "test:coverage": "vitest run --coverage"
       ```

    5. Create `vitest.config.ts` at project root with this exact content:
       ```typescript
       import { defineConfig } from 'vitest/config';
       import react from '@vitejs/plugin-react';
       import tsconfigPaths from 'vite-tsconfig-paths';

       export default defineConfig({
         plugins: [tsconfigPaths(), react()],
         test: {
           environment: 'node',
           include: ['src/**/*.test.ts', 'src/**/*.test.tsx'],
           coverage: {
             provider: 'v8',
             include: ['src/lib/engine/**/*.ts'],
             exclude: ['src/lib/engine/**/*.test.ts', 'src/lib/engine/**/types.ts'],
             thresholds: {
               statements: 100,
               branches: 100,
               functions: 100,
               lines: 100,
             },
           },
         },
       });
       ```

    6. Update `next.config.mjs` to add serverComponentsExternalPackages:
       ```javascript
       /** @type {import('next').NextConfig} */
       const nextConfig = {
         experimental: {
           serverComponentsExternalPackages: ['puppeteer-core', '@sparticuz/chromium'],
         },
       };
       export default nextConfig;
       ```

    7. Create `.env.local.example` with all required env vars:
       ```
       # Supabase
       NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
       NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
       SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

       # Admin seed
       ADMIN_EMAIL=admin@dealstack.app
       ADMIN_PASSWORD=change-me-on-first-deploy

       # Vercel (set automatically on Vercel)
       # VERCEL_ENV=preview
       ```

    8. Ensure `.gitignore` includes: `node_modules/`, `.env.local`, `.env*.local`, `.next/`, `out/`.

    9. Replace default `src/app/page.tsx` with a minimal redirect to `/login`:
       ```typescript
       import { redirect } from 'next/navigation';
       export default function Home() {
         redirect('/login');
       }
       ```

    10. Initialize Supabase local project: `npx supabase init` (creates supabase/ directory with config.toml).
  </action>
  <verify>
    <automated>cd /Users/yashshah/Downloads/projects/deal-stack && npm run build 2>&1 | tail -5 && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "next": with version starting with "14"
    - package.json contains "@supabase/supabase-js"
    - package.json contains "@supabase/ssr"
    - package.json contains "decimal.js"
    - package.json contains "zod"
    - package.json contains "vitest" in devDependencies
    - package.json scripts contains "test": "vitest run"
    - vitest.config.ts contains "defineConfig"
    - vitest.config.ts contains "thresholds"
    - vitest.config.ts contains "statements: 100"
    - next.config.mjs contains "serverComponentsExternalPackages"
    - next.config.mjs contains "puppeteer-core"
    - .env.local.example contains "NEXT_PUBLIC_SUPABASE_URL"
    - .env.local.example contains "SUPABASE_SERVICE_ROLE_KEY"
    - .env.local.example contains "ADMIN_EMAIL"
    - supabase/config.toml exists
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Next.js 14 project boots, all Phase 1 dependencies installed, Vitest configured with 100% coverage thresholds on engine code, Supabase CLI initialized.</done>
</task>

<task type="auto">
  <name>Task 2: Create complete database migration with all 11 tables and RLS policies</name>
  <files>supabase/migrations/00001_initial_schema.sql</files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md (lines 628-975 for complete migration SQL, lines 264-296 for RLS policy pattern, lines 977-992 for RLS audit query)
  </read_first>
  <action>
    Create `supabase/migrations/00001_initial_schema.sql` with the EXACT schema from RESEARCH.md lines 628-975. This includes:

    **Extensions:**
    - `CREATE EXTENSION IF NOT EXISTS "uuid-ossp";`

    **Enums:**
    - `plan_tier` AS ENUM ('free', 'pro', 'enterprise')
    - `plan_status` AS ENUM ('active', 'trialing', 'past_due', 'canceled')
    - `deal_status` AS ENUM ('prospect', 'analyzed', 'offer_made', 'acquired', 'pass')

    **Tables (11 total):**
    1. `public.users` — extends auth.users with id UUID PK REFERENCES auth.users(id) ON DELETE CASCADE, email TEXT NOT NULL, full_name TEXT, role TEXT NOT NULL DEFAULT 'user', stripe_customer_id TEXT, plan plan_tier NOT NULL DEFAULT 'free', plan_status plan_status NOT NULL DEFAULT 'active', plan_limits JSONB DEFAULT '{}', deals_analyzed_this_month INTEGER NOT NULL DEFAULT 0, properties_tracked INTEGER NOT NULL DEFAULT 0, created_at/updated_at TIMESTAMPTZ
    2. `public.user_settings` — id UUID PK, user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE UNIQUE, all settings columns as NUMERIC types (not FLOAT), target_market TEXT, target_submarkets TEXT[], property_types TEXT[], price_ceiling NUMERIC(12,2), down_payment_pct NUMERIC(5,2), property_tax_rate NUMERIC(5,2) DEFAULT 1.8, mgmt_pct NUMERIC(5,2) DEFAULT 9, vacancy_pct NUMERIC(5,2) DEFAULT 8, maintenance_pct NUMERIC(5,2) DEFAULT 10, capex_pct NUMERIC(5,2) DEFAULT 5, closing_costs_pct NUMERIC(5,2) DEFAULT 2.5, acquisition_goal_count INTEGER DEFAULT 5, acquisition_goal_years INTEGER DEFAULT 5, alert_email TEXT, mortgage_rate_override NUMERIC(5,3), finder_cron_interval INTERVAL DEFAULT '1 day', last_finder_run TIMESTAMPTZ, notification_preferences JSONB DEFAULT '{}', logo_url TEXT, accent_color TEXT
    3. `public.properties` — id UUID PK, user_id UUID NOT NULL REFERENCES auth.users(id), address TEXT NOT NULL, city/state/zip/county TEXT, lat/long NUMERIC, census_tract_fips TEXT, property details, list_price/estimated_value NUMERIC(12,2), estimated_rent NUMERIC(10,2), source TEXT, raw_data JSONB
    4. `public.deals` — id UUID PK, user_id UUID, property_id UUID REFERENCES properties(id), status deal_status DEFAULT 'prospect', deal_score NUMERIC(5,1), verdict TEXT, is_fixer_upper BOOLEAN DEFAULT false, renovation_cost NUMERIC(12,2) DEFAULT 0, arv NUMERIC(12,2), notes TEXT
    5. `public.pipeline_stages` — id UUID PK, user_id UUID, deal_id UUID REFERENCES deals(id), from_status deal_status, to_status deal_status NOT NULL, changed_at TIMESTAMPTZ
    6. `public.offers` — id UUID PK, user_id UUID, deal_id UUID REFERENCES deals(id), offer_price NUMERIC(12,2) NOT NULL, offer_date DATE NOT NULL, outcome TEXT, notes TEXT
    7. `public.comparables` — id UUID PK, user_id UUID, property_id UUID REFERENCES properties(id), comp_address TEXT NOT NULL, comp_type TEXT NOT NULL, price/rent/sqft/bedrooms/bathrooms/distance, sold_date DATE, source TEXT, raw_data JSONB
    8. `public.analysis_results` — id UUID PK, user_id UUID, deal_id UUID REFERENCES deals(id), agent_type TEXT NOT NULL, status TEXT DEFAULT 'pending', result_data JSONB, error_message TEXT, started_at/completed_at TIMESTAMPTZ
    9. `public.pdf_reports` — id UUID PK, user_id UUID, deal_id UUID REFERENCES deals(id), report_type TEXT NOT NULL, storage_path TEXT NOT NULL, file_size_bytes INTEGER, generated_at TIMESTAMPTZ
    10. `public.api_cache` — id UUID PK, provider TEXT NOT NULL, endpoint TEXT NOT NULL, cache_key TEXT NOT NULL UNIQUE, response_data JSONB NOT NULL, created_at TIMESTAMPTZ, expires_at TIMESTAMPTZ NOT NULL. Indexes on cache_key and expires_at.
    11. `public.api_usage_log` — id UUID PK, provider TEXT NOT NULL, endpoint TEXT NOT NULL, user_id UUID REFERENCES auth.users(id) ON DELETE SET NULL (nullable!), cache_hit BOOLEAN NOT NULL DEFAULT false, called_at TIMESTAMPTZ. Index on (provider, called_at).

    **RLS Policies:**
    - User-scoped tables (users, user_settings, properties, deals, pipeline_stages, offers, comparables, analysis_results, pdf_reports): `ALTER TABLE x ENABLE ROW LEVEL SECURITY` + SELECT/INSERT/UPDATE/DELETE policies with `auth.uid() = user_id` (or `auth.uid() = id` for users table). Note: users table only needs SELECT + UPDATE (no INSERT/DELETE by user). pipeline_stages only needs SELECT + INSERT.
    - Global tables (api_cache, api_usage_log): `ENABLE ROW LEVEL SECURITY` + `FOR SELECT TO authenticated USING (true)` policy only (service role handles writes).

    **Triggers:**
    - `update_updated_at()` function + triggers on: users, user_settings, properties, deals, offers.

    Copy the SQL EXACTLY from RESEARCH.md. Do NOT modify column types, defaults, or policy names.
  </action>
  <verify>
    <automated>wc -l /Users/yashshah/Downloads/projects/deal-stack/supabase/migrations/00001_initial_schema.sql</automated>
  </verify>
  <acceptance_criteria>
    - File supabase/migrations/00001_initial_schema.sql exists and has > 200 lines
    - File contains "CREATE TABLE public.users"
    - File contains "CREATE TABLE public.user_settings"
    - File contains "CREATE TABLE public.properties"
    - File contains "CREATE TABLE public.deals"
    - File contains "CREATE TABLE public.pipeline_stages"
    - File contains "CREATE TABLE public.offers"
    - File contains "CREATE TABLE public.comparables"
    - File contains "CREATE TABLE public.analysis_results"
    - File contains "CREATE TABLE public.pdf_reports"
    - File contains "CREATE TABLE public.api_cache"
    - File contains "CREATE TABLE public.api_usage_log"
    - File contains "ENABLE ROW LEVEL SECURITY" at least 11 times (once per table)
    - File contains "CREATE POLICY" at least 30 times
    - File contains "auth.uid()" in policy definitions
    - File contains "REFERENCES auth.users(id) ON DELETE CASCADE"
    - File contains "plan_tier AS ENUM"
    - File contains "deal_status AS ENUM"
    - File contains "NUMERIC(12,2)" for dollar amounts (not FLOAT or REAL)
    - File contains "update_updated_at" trigger function
    - api_cache table has "cache_key TEXT NOT NULL UNIQUE"
    - api_usage_log has "user_id UUID REFERENCES auth.users(id) ON DELETE SET NULL" (nullable)
  </acceptance_criteria>
  <done>Complete database migration with all 11 tables, proper column types (NUMERIC not FLOAT), RLS enabled on every table, user-scoped policies using auth.uid(), global tables with authenticated-read policies, and updated_at triggers.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0 (Next.js compiles)
- `npx vitest run` exits 0 (Vitest runs, no tests yet is OK)
- supabase/migrations/00001_initial_schema.sql contains all 11 CREATE TABLE statements
- Every table has ENABLE ROW LEVEL SECURITY
- grep -c "CREATE POLICY" on migration file returns >= 30
</verification>

<success_criteria>
Next.js 14 project is fully scaffolded with all Phase 1 dependencies. Database migration file contains all 11 tables with correct column types, RLS enabled on every table, and policies enforcing auth.uid() access control. Vitest is configured and ready for TDD in Plan 01-04.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
